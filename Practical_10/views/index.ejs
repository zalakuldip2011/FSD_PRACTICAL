<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: Arial, sans-serif;
            background: #f5f5f5;
            color: #333;
            line-height: 1.6;
        }
        
        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }
        
        header {
            background: #fff;
            border-radius: 12px;
            padding: 2rem;
            margin-bottom: 2rem;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            text-align: center;
        }
        
        .header-title {
            font-size: 2.5rem;
            font-weight: 600;
            color: #333;
            margin-bottom: 0.5rem;
        }
        
        .header-subtitle {
            color: #666;
            font-size: 1.1rem;
        }
        
        .status-indicator {
            display: inline-block;
            padding: 0.5rem 1rem;
            background: #d4edda;
            color: #155724;
            border-radius: 20px;
            font-weight: 500;
            margin-top: 1rem;
        }
        
        .dashboard-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 1.5rem;
            margin-bottom: 2rem;
        }
        
        .metric-card {
            background: #fff;
            border-radius: 12px;
            padding: 2rem;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            text-align: center;
            transition: transform 0.2s ease;
        }
        
        .metric-card:hover {
            transform: translateY(-5px);
        }
        
        .metric-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            display: block;
        }
        
        .metric-title {
            font-size: 1rem;
            color: #666;
            margin-bottom: 0.5rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .metric-value {
            font-size: 2.5rem;
            font-weight: 600;
            color: #333;
            margin-bottom: 0.5rem;
        }
        
        .metric-unit {
            font-size: 1rem;
            color: #999;
        }
        
        .progress-bar {
            background: #e9ecef;
            border-radius: 10px;
            height: 8px;
            margin: 1rem 0;
            overflow: hidden;
        }
        
        .progress-fill {
            height: 100%;
            background: #28a745;
            border-radius: 10px;
            transition: width 0.3s ease;
        }
        
        .progress-fill.warning {
            background: #ffc107;
        }
        
        .progress-fill.danger {
            background: #dc3545;
        }
        
        .activities-section {
            background: #fff;
            border-radius: 12px;
            padding: 2rem;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            margin-bottom: 2rem;
        }
        
        .section-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
            color: #333;
        }
        
        .activity-list {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }
        
        .activity-item {
            display: flex;
            align-items: center;
            padding: 1rem;
            background: #f8f9fa;
            border-radius: 8px;
            border-left: 4px solid #28a745;
        }
        
        .activity-icon {
            font-size: 1.5rem;
            margin-right: 1rem;
        }
        
        .activity-details {
            flex: 1;
        }
        
        .activity-action {
            font-weight: 500;
            color: #333;
        }
        
        .activity-time {
            font-size: 0.9rem;
            color: #666;
            margin-left: auto;
        }
        
        .refresh-btn {
            background: #28a745;
            color: #fff;
            border: none;
            padding: 1rem 2rem;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.2s ease;
            display: block;
            margin: 2rem auto;
        }
        
        .refresh-btn:hover {
            background: #218838;
            transform: translateY(-2px);
        }
        
        .last-updated {
            text-align: center;
            color: #666;
            font-size: 0.9rem;
            margin-top: 1rem;
        }
        
        .quick-actions {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 1rem;
            margin-top: 2rem;
        }
        
        .action-btn {
            background: #007bff;
            color: #fff;
            border: none;
            padding: 1rem;
            border-radius: 8px;
            text-decoration: none;
            text-align: center;
            font-weight: 500;
            transition: all 0.2s ease;
            display: block;
        }
        
        .action-btn:hover {
            background: #0056b3;
            transform: translateY(-2px);
        }
        
        .action-btn.secondary {
            background: #6c757d;
        }
        
        .action-btn.secondary:hover {
            background: #5a6268;
        }
        
        @media (max-width: 768px) {
            .header-title {
                font-size: 2rem;
            }
            
            .dashboard-grid {
                grid-template-columns: 1fr;
            }
            
            .container {
                padding: 10px;
            }
            
            .activities-section, header, .metric-card {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header>
            <h1 class="header-title">🖥️ Server Monitoring Dashboard</h1>
            <p class="header-subtitle">Real-time server performance and system health monitoring</p>
            <div class="status-indicator">🟢 System Status: <%= serverData.serverStatus.toUpperCase() %></div>
        </header>

        <div class="dashboard-grid">
            <div class="metric-card">
                <span class="metric-icon">⏱️</span>
                <div class="metric-title">Uptime</div>
                <div class="metric-value"><%= Math.floor(serverData.uptime / 60) %></div>
                <div class="metric-unit">hours <%= serverData.uptime % 60 %> minutes</div>
            </div>

            <div class="metric-card">
                <span class="metric-icon">🧠</span>
                <div class="metric-title">CPU Usage</div>
                <div class="metric-value"><%= serverData.cpuUsage %></div>
                <div class="metric-unit">percent</div>
                <div class="progress-bar">
                    <div class="progress-fill <%= serverData.cpuUsage > 70 ? 'danger' : serverData.cpuUsage > 50 ? 'warning' : '' %>" 
                         data-width="<%= serverData.cpuUsage %>"></div>
                </div>
            </div>

            <div class="metric-card">
                <span class="metric-icon">💾</span>
                <div class="metric-title">Memory Usage</div>
                <div class="metric-value"><%= serverData.memoryUsage %></div>
                <div class="metric-unit">percent</div>
                <div class="progress-bar">
                    <div class="progress-fill <%= serverData.memoryUsage > 70 ? 'danger' : serverData.memoryUsage > 50 ? 'warning' : '' %>" 
                         data-width="<%= serverData.memoryUsage %>"></div>
                </div>
            </div>

            <div class="metric-card">
                <span class="metric-icon">💿</span>
                <div class="metric-title">Disk Usage</div>
                <div class="metric-value"><%= serverData.diskUsage %></div>
                <div class="metric-unit">percent</div>
                <div class="progress-bar">
                    <div class="progress-fill <%= serverData.diskUsage > 70 ? 'danger' : serverData.diskUsage > 50 ? 'warning' : '' %>" 
                         data-width="<%= serverData.diskUsage %>"></div>
                </div>
            </div>

            <div class="metric-card">
                <span class="metric-icon">🔗</span>
                <div class="metric-title">Active Connections</div>
                <div class="metric-value"><%= serverData.activeConnections %></div>
                <div class="metric-unit">connections</div>
            </div>

            <div class="metric-card">
                <span class="metric-icon">📊</span>
                <div class="metric-title">Requests/Min</div>
                <div class="metric-value"><%= serverData.requestsPerMinute %></div>
                <div class="metric-unit">requests</div>
            </div>

            <div class="metric-card">
                <span class="metric-icon">⚡</span>
                <div class="metric-title">Response Time</div>
                <div class="metric-value"><%= serverData.responseTime %></div>
                <div class="metric-unit">milliseconds</div>
            </div>

            <div class="metric-card">
                <span class="metric-icon">🌐</span>
                <div class="metric-title">Network I/O</div>
                <div class="metric-value"><%= serverData.networkIn %>↓ / <%= serverData.networkOut %>↑</div>
                <div class="metric-unit">KB/s</div>
            </div>
        </div>

        <div class="activities-section">
            <h2 class="section-title">📋 Recent Activities</h2>
            <div class="activity-list">
                <% activities.forEach(activity => { %>
                    <div class="activity-item">
                        <span class="activity-icon"><%= activity.icon %></span>
                        <div class="activity-details">
                            <div class="activity-action"><%= activity.action %></div>
                        </div>
                        <div class="activity-time"><%= activity.time %></div>
                    </div>
                <% }) %>
            </div>
        </div>

        <button class="refresh-btn" onclick="refreshData()">🔄 Refresh Data</button>
        
        <div class="quick-actions">
            <a href="/logs" class="action-btn">View Logs</a>
            <a href="#" class="action-btn secondary">Server Settings</a>
            <a href="#" class="action-btn secondary">Performance Reports</a>
            <a href="#" class="action-btn secondary">System Tools</a>
        </div>

        <div class="last-updated">Last updated: <%= serverData.lastUpdated %></div>
    </div>

    <script>
        // Set progress bar widths on page load
        document.addEventListener('DOMContentLoaded', function() {
            const progressElements = document.querySelectorAll('.progress-fill[data-width]');
            progressElements.forEach(element => {
                const width = element.getAttribute('data-width');
                element.style.width = width + '%';
            });
        });

        function refreshData() {
            fetch('/api/status')
                .then(response => response.json())
                .then(data => {
                    location.reload();
                })
                .catch(error => {
                    console.error('Error fetching data:', error);
                });
        }

        // Auto-refresh every 30 seconds
        setInterval(refreshData, 30000);
    </script>
</body>
</html>
