<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Library Portal - Login</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <div class="card">
      <div class="header">
        <div class="icon">🔐</div>
        <h1>Library Portal Login</h1>
        <p class="subtitle">Access your reading dashboard</p>
      </div>

      <% if (error) { %>
        <div class="error-message">
          <strong>⚠️ Login Error:</strong> <%= error %>
        </div>
      <% } %>

      <form action="/login" method="POST">
        <div class="form-group">
          <label for="username">Username</label>
          <input 
            type="text" 
            id="username" 
            name="username" 
            placeholder="Enter your username"
            required
            autofocus
          >
          <small>Enter any username to login</small>
        </div>

        <div class="form-group">
          <label for="email">Email Address</label>
          <input 
            type="email" 
            id="email" 
            name="email" 
            placeholder="your.email@example.com"
            required
          >
          <small>We'll use this to contact you about your account</small>
        </div>

        <button type="submit" class="btn-submit">
          <span>Login to Library</span>
          <span class="arrow">→</span>
        </button>
      </form>

      <div class="divider">
        <span>or</span>
      </div>

      <div class="action-buttons">
        <a href="/" class="btn-back">Back to Home</a>
      </div>

      <div class="info-box">
        <p><strong>Demo Mode:</strong> This is a session demonstration. Enter any username and valid email to login.</p>
      </div>
    </div>
  </div>
</body>
</html>