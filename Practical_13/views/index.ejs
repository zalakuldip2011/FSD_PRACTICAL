<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tax Income Calculator</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <div class="card">
      <h1>Tax Income Calculator</h1>
      <p class="subtitle">Enter your income from two sources to calculate total income</p>

      <% if (error) { %>
        <div class="error-message">
          <strong>Error:</strong> <%= error %>
        </div>
      <% } %>

      <form action="/calculate" method="POST">
        <div class="form-group">
          <label for="income1">Income Source 1 (₹)</label>
          <input 
            type="text" 
            id="income1" 
            name="income1" 
            placeholder="Enter amount (e.g., 50000)"
            required
          >
          <small>Enter your primary income source</small>
        </div>

        <div class="form-group">
          <label for="income2">Income Source 2 (₹)</label>
          <input 
            type="text" 
            id="income2" 
            name="income2" 
            placeholder="Enter amount (e.g., 25000)"
            required
          >
          <small>Enter your secondary income source</small>
        </div>

        <button type="submit" class="btn-submit">Calculate Total Income</button>
      </form>
    </div>
  </div>
</body>
</html>